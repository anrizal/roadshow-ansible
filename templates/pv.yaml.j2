apiVersion: "v1"
kind: "PersistentVolume"
metadata:
  name: "{{ pv_name }}" 
  labels:
    owner: "{{ owner }}"
spec:
  capacity:
    storage: "{{ capacity }}Gi" 
  accessModes:
    - ReadWriteOnce
  awsElasticBlockStore: 
    fsType: "ext4" 
    volumeID: "{{ volid }}"
